#include<stdio.h>#define MAX 20int a[MAX][MAX], reach[MAX], n; void dfs(int v) {    int i;    reach[v] = 1;    printf("%d ", v);    for (i = 1; i <= n; i++) {        if (a[v][i] && !reach[i]) {            dfs(i);        }    }}int main() {    int i, j, edges, u, v, start, count = 0;        printf("Enter the number of vertices: ");    scanf("%d", &n);     printf("Enter the number of edges: ");    scanf("%d", &edges);     for (i = 1; i <= n; i++) {        reach[i] = 0;        for (j = 1; j <= n; j++) {            a[i][j] = 0;        }    }     printf("Enter the edges in the format (u v):\n");    for (i = 1; i <= edges; i++) {        printf("Edge %d: ", i);        scanf("%d %d", &u, &v);        if (u >= 1 && u <= n && v >= 1 && v <= n) {            a[u][v] = 1;              a[v][u] = 1;          } else {            printf("Invalid edge! Nodes must be between 1 and %d.\n", n);            i--;           }    }        printf("Enter the starting node: ");    scanf("%d", &start);     printf("\nDFS Traversal starting from node %d:\n", start);    dfs(start);       for (i = 1; i <= n; i++) {        if (!reach[i]) {            dfs(i);        }    }     for (i = 1; i <= n; i++) {        if (reach[i])            count++;    }    if (count == n)        printf("\n\nGraph is connected.\n");    else        printf("\n\nGraph is not connected.\n");    return 0;}